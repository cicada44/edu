#include <iostream>
#include <string>
#include <vector>

using namespace std;
using std::string;
using std::vector;

bool compare(const matrix& m1, const matrix& m2);
vector<int> change_val(int value, vector<int> iter);

int main()
{
    return 0;
}